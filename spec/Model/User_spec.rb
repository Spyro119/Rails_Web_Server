require 'rails_helper'

RSpec.describe User, :type => :model do 
    # it 'is valid with valid attributes'
    # it { expect(User).to validate_presence_of(:strong_password) }
    # it 'is not valid whitout a strong password'
end